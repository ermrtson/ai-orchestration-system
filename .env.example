# OpenAI API Key for GPT services (required for citation, classifier, and quality agents)
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic API Key for Claude services (required for summarizer agent)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Database connection (optional - defaults are in docker-compose.yml)
DATABASE_URL=postgresql://llmuser:llmpassword@postgres/llmorchestrator

# Celery settings (optional - defaults are in docker-compose.yml)
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# Frontend URL (optional - defaults are in the code)
REACT_APP_API_URL=http://localhost:8000
